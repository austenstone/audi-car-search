import { FilterMileagePipe } from './filter-mileage.pipe';

describe('FilterMileagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMileagePipe();
    expect(pipe).toBeTruthy();
  });
});

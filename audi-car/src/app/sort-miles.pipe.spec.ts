import { SortMilesPipe } from './sort-miles.pipe';

describe('SortMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
